# 0.2.0 — Cloud PBX Phone Theme + Branding

- Product display identity is now **Cloud PBX Phone**.
- The established dark theme remains available and is still the default.
- A complete light theme is available from the topbar Sun/Moon toggle.
- Theme preference is remembered locally in the browser.
- Existing brand logo and favicon assets under `assets/brand/` are now used by the application.
- PBX call workflows and mock behavior are unchanged.
