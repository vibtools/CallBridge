# Cloud PBX Phone 0.3.1

Call-runtime reliability update. Incoming scheduling now persists across refreshes, elapsed call time is derived from timestamps, background time is reconciled when the app becomes active again, startup calls use stable active lifetimes, agent/queue state follows active calls, and long inactive gaps avoid catch-up floods.
